import { TravelAdvice } from "@/types";
import { useEffect, useState } from "react";

export default function EvChargingStations() {
  const [messages, setMessages] = useState<TravelAdvice[]>([]);

  useEffect(() => {
    
  },[]);
  return <div>Ev charging stations</div>;
}
